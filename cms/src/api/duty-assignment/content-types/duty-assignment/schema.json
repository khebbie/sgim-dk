{
  "kind": "collectionType",
  "info": {
    "singularName": "duty-assignment",
    "pluralName": "duty-assignments",
    "displayName": "Duty Assignment",
    "description": "Who is assigned to a duty at an event. A row exists only once assigned; the assignee is a free-text name."
  },
  "options": { "draftAndPublish": false },
  "pluginOptions": {},
  "attributes": {
    "event": { "type": "relation", "relation": "manyToOne", "target": "api::event.event" },
    "category": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::duty-category.duty-category"
    },
    "assignee": { "type": "string" },
    "note": { "type": "text" }
  }
}
